import ast
import os


def create_site_map(args):
    """
    Wrapper function to parse the sitemap result
    :param args: str representing the arguments in the form of a list
    :return: list with the site map
    """
    print(args)
    return 1
